{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "composite": true // Often used in monorepos for project references
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
