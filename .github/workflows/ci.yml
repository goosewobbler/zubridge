name: CI

on:
  push:
    branches:
      - main
    tags:
      - v[0-9]+.[0-9]+.[0-9]+*
  pull_request:

env:
  TURBO_TELEMETRY_DISABLED: 1
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
  TURBO_API: ${{ secrets.TURBO_API }}

jobs:
  # Build shared packages on Linux only
  build-shared-packages:
    name: Build Shared Packages
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🛠️ Setup workspace
        uses: ./.github/workflows/actions/setup-workspace
        with:
          node-version: "20.x"

      - name: 🏗️ Build shared packages
        run: pnpm run build:packages

      - name: 📦 Upload shared packages
        uses: actions/upload-artifact@v4
        with:
          name: shared-packages
          path: |
            packages/*/dist
            packages/tauri-plugin
          retention-days: 1


  # Code quality checks - Ubuntu only (fast feedback)
  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest
    needs: [build-shared-packages]
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🛠️ Setup workspace
        uses: ./.github/workflows/actions/setup-workspace
        with:
          node-version: "20.x"

      - name: 📦 Download shared packages
        uses: actions/download-artifact@v4
        with:
          name: shared-packages
          path: packages

      - name: 🔍 Run Linting
        run: pnpm run lint

      - name: 🎨 Check Formatting
        run: pnpm run format:check

      - name: ✅ Run Typecheck
        run: pnpm run typecheck

      - name: 🧪 Run Unit Tests
        run: pnpm test:unit

  # E2E + Package tests - Full test suite
  e2e-and-package-tests:
    needs: [build-shared-packages, code-quality]
    name: ${{ matrix.app }}:${{ matrix.mode }}
    strategy:
      matrix:
        app: [electron, tauri, tauri-v1]
        mode: [zustand-basic, zustand-handlers, zustand-reducers, redux, custom]
        exclude:
          # Exclude non-basic modes for tauri
          - app: tauri
            mode: zustand-handlers
          - app: tauri
            mode: zustand-reducers
          - app: tauri
            mode: redux
          - app: tauri
            mode: custom
          # Exclude non-basic modes for tauri-v1
          - app: tauri-v1
            mode: zustand-handlers
          - app: tauri-v1
            mode: zustand-reducers
          - app: tauri-v1
            mode: redux
          - app: tauri-v1
            mode: custom
      fail-fast: false
    uses: ./.github/workflows/build-and-test.yml
    with:
      app: ${{ matrix.app }}
      mode: ${{ matrix.mode }}
      package_test_only: false
    secrets:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
      TURBO_API: ${{ secrets.TURBO_API }}

  # Package-only tests - Test minimal apps without E2E suite
  package-only-tests:
    needs: [build-shared-packages, code-quality]
    name: ${{ matrix.app }}:${{ matrix.mode }}
    strategy:
      matrix:
        app: [electron]
        mode: [sandbox-true]
      fail-fast: false
    uses: ./.github/workflows/build-and-test.yml
    with:
      app: ${{ matrix.app }}
      mode: ${{ matrix.mode }}
      package_test_only: true
    secrets:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ secrets.TURBO_TEAM }}
      TURBO_API: ${{ secrets.TURBO_API }}

  # Final status job
  ci-status:
    name: CI Status
    needs: [
      build-shared-packages,
      code-quality,
      e2e-and-package-tests,
      package-only-tests
    ]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check status
        run: |
          # Check if any job succeeded
          if [[ "${{ contains(needs.*.result, 'success') }}" == "true" ]]; then
            echo "At least one job passed successfully"
            exit 0
          else
            echo "All jobs failed"
            exit 1
          fi
